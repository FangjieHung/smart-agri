using Shouldly;

namespace SmartAgri.Domain.Tests;

/// <summary>
/// Smoke test proving the test project builds and runs. The Domain project
/// has no types yet in this milestone slice; real domain tests land once
/// entities are introduced.
/// </summary>
public class PlaceholderTests
{
    [Fact]
    public void Domain_test_harness_runs()
    {
        true.ShouldBeTrue();
    }
}
